/target
**/*.rs.bk
*.pdb
.DS_Store

# Local runtime assets — never commit ROMs, BIOS, cores or player data
/cores/
/roms/
/saves/
/system/
*.sfc
*.smc
*.fig
*.swc
*.dylib
*.so
*.dll
*.srm
*.state
*.state[0-9]
shot-*.bmp

# Config and secrets (the app's default config lives in ~/.config)
config.toml
.env
screenscraper.toml
